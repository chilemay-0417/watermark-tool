# 更新日志

## 2.0.0 — 2026-09-09

以下变化均以 **1.1.0** 为基准。由于调整了输入格式和色彩处理范围，本版采用主版本号 2.0.0。

### 主要变化

- **原始尺寸布局**：增加 `--output-mode original`，保留每张照片旋正后的原始像素尺寸，顶部对齐，画布随照片、水印和日期扩展。
- **色彩管理**：默认 `--color-mode preserve` 保留来源 RGB 色域，混合 SDR 色域转为 ProPhoto RGB；支持 ICC、PNG cICP 等色彩标记。可选 `--color-mode srgb` 输出 8 位 SDR sRGB。
- **高位深 PNG**：读取 PNG 原始整数样本及 HEIF 原生 10/12 位样本，以高精度转换、缩放后再量化；支持无损 16 位 PNG 及透明度保留。高位深来源导出 JPEG 时提示降为 8 位。
- **元数据保留**：从不保留原始 EXIF 改为默认 `--metadata safe`，保留筛选后的拍摄信息、作者、版权及单图 DPI 等，重建尺寸和方向，去除旧缩略图、MakerNote 和设备序列号。多图仅保留共同信息；`--metadata none` 删除非色彩元数据。
- **GPS 导出开关**：增加 `--preserve-gps`，默认关闭，仅控制 safe 模式下单图 EXIF GPS 的保留，与地点联网查询独立。
- **日期修正**：只显示有效的 EXIF 拍摄时间，精确到秒；缺失或无效时省略，不再回退到其他时间，不显示小数秒和时区。
- **批处理与速度**：增加 `--batch`；Finder 逐张导出使用单个 Python 进程，单张失败后继续并汇总结果。缓存处理后的 Logo 和签名，使用原生 PNG 解码，逐张释放原图并复用元数据，减少重复计算和内存占用。
- **稳定性**：完善 original 窄图日期和水印边界处理；避免仅 ICC 描述不同导致不必要的色域转换；地点查询共享超时预算，后续失败时保留已获取的地名。
- **Finder 环境**：优先尝试项目 `.venv/bin/python`，可通过 `WATERMARK_PYTHON_BIN` 指定优先使用的 Python。

### 兼容性变化

- 不再支持 RAW 显影和 TIFF 输入（包括 TIFF Logo / 签名）；请先转换为 SDR PNG 或 JPEG。
- 仅处理 SDR，检测到 PQ / HLG 或已知 HDR 增益图时会报错，不提供 HDR 转换或输出。
- 损坏或不匹配的 ICC、不支持的色彩标记会明确报错；默认保留色域模式不接受 CMYK / Lab，带有效 ICC 的图片可用 `--color-mode srgb` 转换。
- 依赖增加 NumPy、pyspng，Pillow 最低版本提高至 12.2，移除 rawpy；Python 要求仍为 3.10+。

### 从 1.1.0 升级

1. 备份自行修改的配置、签名、Logo 和品牌规则，更新整个项目文件夹。按需迁移配置，避免直接用旧 `config.py` 覆盖当前文件。
2. 在项目目录安装依赖；没有虚拟环境时先运行 `python3 -m venv .venv`：

   ```bash
   source .venv/bin/activate
   python -m pip install -r requirements.txt
   ```

3. 原有命令行入口和 Finder 脚本路径保留。项目路径不变时无需重建快速操作；移动目录后请更新操作中的路径，并在新位置重建虚拟环境。RAW / TIFF / HDR 照片需先转换为受支持的 SDR 格式。
4. 用样张验证环境：

   ```bash
   python layout.py samples/horizontal1.jpg --output-mode original --include-gps-location false -o output/upgrade-check.png
   ```

默认仍为 video 4K、JPEG 100%、4:4:4 色度采样，地点联网查询仍开启。元数据现在默认保留筛选后的字段，但不导出 GPS；需要删除非色彩元数据时使用 `--metadata none`。命令与参数见 [使用指南](docs/usage.md#命令与参数)。

### 验证记录

2.0.0 发布前在 macOS / Python 3.14.2 环境中通过 131 项自动测试、Ruff 和 Finder 脚本语法检查。覆盖原始尺寸、高位深 PNG、色彩、元数据、日期、批量失败继续处理及缓存恢复等；未在其他 Python 版本或操作系统上复测。

## 1.1.0 — 2026-09-09

### 新增与调整

- 修复快门时长显示：避免将 `0.8s`、`0.4s` 错写为 `1/1s`、`1/2s`，保留 `1.25s` 等小数秒；倒数表示的相对误差限制在 0.1% 内。
- 焦距优先显示有效的 35mm 等效焦距，缺失、为 0 或无效时回退到实际焦距，两者都无效时省略。
- video 单张照片左右留白相等；多张照片的左右留白和所有图间距相等。
- video 自动寻找能放下照片和水印的最大整数照片高度，保留固定 3840 × 2160 画布；底部横线和日期保留 1.0 的设置，不随照片缩小。
- 防重叠计算包含最右图的两侧占用，以及每对相邻照片的 Logo、签名、参数和地点文字；video 初始图间距小于占用之和 `a` 时，自动缩小照片直到间距至少达到 `1.5 × a`；不重叠时保留原高度。
- adaptive 多图间距、左留白和右留白均为上留白的 1.8 倍（取整到像素），取消张数限制，画布宽度随照片增长。
- Finder 合成入口同步支持 adaptive 多图；video 仍最多 3 张。
- 默认输出文件名过长时自动缩短；超宽 JPEG 提示改用 PNG。

### 优化

- 确定最终布局后仅缩放原图一次，避免反复缩放。
- 普通图片先读取尺寸，绘制时逐张解码并释放，避免同时保留所有原图。
- 文字图片只生成一次，同时用于测量和绘制。
- 每次合成只加载一次品牌匹配规则，统一水印计划和间距计算。
- Finder 入口共用 Python 检查和通知逻辑，失败返回正确状态，文件名通过参数安全传入系统通知。
- 阻止覆盖输入原图；输出先完整写入临时文件再替换，避免失败时损坏已有输出。
- 校验无效 EXIF、GPS、非有限间距及文字边界，移除 Pillow 10+ 已不需要的兼容分支。
- 渲染不回写调用者的配置，允许同一份配置重复使用。
- 保留原有 Finder 脚本路径和命令行参数，无需重建快速操作。

### 文档与展示

- 更新 samples 展示成片，按 video 三个场景、adaptive 三个场景重写 README。
- README 使用独立压缩预览并链接完整成片，新增预览生成脚本。

### 验证

- 自动测试覆盖单图、双图、三图、混合比例、临界重叠、宽 Logo / 地点文字、无水印、EXIF 旋转、长文件名以及 adaptive 多图与超宽布局。
- 从实际导出像素检查 video 等间距和 adaptive 的 1.8 倍上留白间距。
- 使用现有样张验证单图、混合三图 video 和多图 adaptive；Finder 入口以模拟运行验证成功、失败及特殊文件名。
- 回归测试覆盖输出原图保护、保存失败恢复、上下边界及无效参数；69 项自动测试、Ruff 检查及 zsh 语法检查通过。

### 从 1.0 升级

更新整个项目文件夹，包括 `scripts/`、`src/` 和 `assets/`，并重新安装 `requirements.txt` 中的依赖。自行修改过的配置、签名和 Logo 请先备份，再按需迁移。项目路径不变时，原有 Finder 快速操作可继续使用。

## 1.0.0

- 初始版本：照片水印、品牌 Logo、签名、video / adaptive 布局及 Finder 快速操作。
