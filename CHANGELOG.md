# 更新日志

## 2.0.0 — 2026-09-09

相较 1.1.0，本版新增原始尺寸输出、高精度色彩处理及可配置的元数据保留。由于移除了原有 RAW 和 TIFF 输入支持，并收紧了可接受的色彩输入范围，采用主版本号 2.0.0。以下记录以已发布的 1.1.0 为比较基准。

### 兼容性变化

- 移除 RAW 显影及 `rawpy` 依赖，NEF、ARW、CR2、CR3、RAF、DNG 等 RAW 输入会明确报错；请先在照片编辑软件中导出 SDR PNG 或 JPEG。
- 明确限定 SDR 处理：检测到 PQ/HLG 或已知 HDR 增益图标记时停止导出，并提示先转换为 SDR。本版不提供 HDR 转换或 HDR 输出。
- 移除 TIFF 输入及 TIFF Logo/签名素材支持；`.tif`、`.tiff` 和改名后的 TIFF 文件均明确报错，请先转换为 PNG 或 JPEG。项目不再依赖 `tifffile` 或 `imagecodecs`，Finder 同步移除对应依赖检查。
- 损坏或不匹配的 ICC、不支持的 cICP 会明确报错。默认保留色域路径不接受 CMYK/Lab；带有效 ICC 的 CMYK/Lab 可显式选择 `--color-mode srgb`。
- 导出从不保留原始 EXIF 改为默认 `--metadata safe`，保留筛选后的拍摄信息及作者、版权等字段；需要去除非色彩元数据时使用 `--metadata none`。

### 新增与优化

- 新增 `--output-mode original`：保留每张照片旋正后的原始像素尺寸，顶部对齐，画布随照片和水印扩展，`--height` 不参与照片缩放。
- 新增默认 `--color-mode preserve`：相同 RGB 色彩定义沿用来源色域，混合 SDR 色域转换到 ProPhoto RGB 合成；输出嵌入相应色彩配置。新增 `--color-mode srgb`，供需要 8 位 sRGB 输出的分享场景使用。
- PNG 读取原始整数样本，HEIF 保留原生 10/12 位样本；以高精度完成色彩转换及缩放，最终输出时才量化，减少暗部和高位深细节损失。
- 新增无损 16 位 PNG 输出：保留色域模式下，高位深来源或混合色域合成自动选择 16 位 PNG。相同 RGB 定义、无缩放的 PNG 照片区域保留解码后的原始 RGB 样本；混合色域转换不能视为逐像素无损。
- 识别 ICC、PNG cICP、sRGB、EXIF Adobe RGB 及 PNG gAMA/cHRM 色彩定义。广色域成片需用支持 ICC 的软件查看。
- 保留色域模式下 PNG 保留照片透明度；JPEG 按画布背景合成透明区域并提示，高位深来源导出 JPEG 时提示降为 8 位。
- 单图保留筛选后的拍摄参数、作者、版权、DPI 及 XMP/IPTC 信息，重建尺寸和方向，去除旧缩略图、MakerNote 和设备序列号；多图仅保留共同的作者、版权及筛选后的共同 XMP。
- 新增 `--preserve-gps`，默认关闭，仅在 `safe` 策略下控制单图 EXIF GPS 导出；与默认开启的地点联网查询 `--include-gps-location` 独立。
- 修复缺失或异常日期被写成当前时间的问题。水印只显示 EXIF `DateTimeOriginal` 拍摄时间，精确到秒，不显示小数秒和时区；拍摄时间缺失或无效时不显示日期，不回退到数字化或修改时间。导出 EXIF 中的有效时区及亚秒字段仍按元数据策略保留。
- 8 位 PNG 照片和 Logo 改用 Pillow 原生解码器，避免逐像素 Python 解码；普通 RGB 图片直接读取所需通道，减少 RGBA 中间副本。16 位 PNG 使用 pyspng 原生解码，保留整数样本、灰度/彩色透明通道及透明色键；pypng 仅用于开发测试。
- Logo 和签名图片缓存完成色彩转换、缩放后的结果，按素材内容、尺寸和处理版本自动失效；缓存损坏或不可写时自动重新处理。
- 新增 `--batch` 逐张导出；Finder 批量入口改为单个 Python 进程，复用字体、水印及地点缓存，单张失败后继续处理其余照片并汇总结果。
- 保留色域路径先检查文件信息，再逐张解码、合成并释放原图；缩放保持浮点精度，量化改为分条处理，减少大尺寸中间副本。
- 拍摄信息与导出元数据复用一次读取的结果；PNG 扫描包括像素块之后的 EXIF/XMP，不为读取元数据再次解码照片。
- original 按每张照片的高度预留签名、Logo 和参数空间，并把日期和横线纳入宽度计算，修复短图排列顺序导致的报错及窄图日期截断。
- ICC 比较忽略版权和描述等非色彩字段，避免相同色域被误判为混合色域及不必要的 16 位输出。
- 地点反查的多次尝试共用超时预算；后续失败时保留此前已查到的地名。
- Finder 在未指定 `WATERMARK_PYTHON_BIN` 时优先检测项目 `.venv/bin/python`，同步检查新的图片解码依赖。
- README 预览生成时转换为 sRGB 并嵌入 ICC；使用指南补充色彩、位深、透明度及元数据策略。

### 从 1.1.0 升级

- 备份自行修改的配置、签名和 Logo，更新整个项目文件夹，并重新安装 `requirements.txt` 中的依赖。新增 NumPy、pyspng，Pillow 最低版本提高至 12.2；Python 要求仍为 3.10+。
- 原有命令行入口、Finder 脚本路径及 `video` / `adaptive` 模式保留；项目路径不变时无需重建快速操作。默认仍为 video 4K、JPEG 100% 和 4:4:4 色度采样，地点联网查询仍默认开启。
- 原有 RAW / TIFF 工作流需先导出受支持的 SDR PNG 或 JPEG。高精度处理逐张解码，但最终画布及当前照片仍需要内存；大批量照片可使用 `--batch` 分别导出。
- 希望避免照片缩放和 JPEG 重编码时，使用 `--output-mode original -o output.png`；JPEG 100% 仍为有损编码。
- 具体命令及升级后的验证步骤见 [使用指南](docs/usage.md#从-110-升级到-200)。

### 验证

- 新增原始像素、16 位 PNG、TIFF 输入拒绝、HEIF 暗部精度、混合色域、HDR 输入拒绝、透明度、日期、元数据及失败恢复回归测试。
- 2026-09-09 在本机 macOS、Python 3.14.2 项目虚拟环境中验证：131 项自动测试、Ruff 检查、Finder 入口及共用脚本的 zsh 语法检查、`git diff --check` 均通过；未在其他 Python 版本或操作系统上复测。
- 与本次审查前的 2.0.0 工作区比较（已包含上一轮 PNG 加速），关闭 GPS 查询、各运行三次取中位数：缓存建立后 phone1 单图 1.15 → 0.94 秒，phone2 单图 1.89 → 0.57 秒，三图合成 3.80 → 1.26 秒；三组 JPEG 解码后的像素全部一致。首次处理仍需建立缓存；完整测量条件见 [运行速度](docs/usage.md#运行速度)。
- 新增缓存失效与恢复、批量失败继续处理、逐张释放原图、元数据不解码、original 边界、ICC 描述差异、GPS 总预算及原生 16 位 PNG 交错扫描/透明通道回归测试。

## 1.1.0 — 2026-09-09

### 新增与调整

- 修复快门时长显示：避免将 `0.8s`、`0.4s` 错写为 `1/1s`、`1/2s`，保留 `1.25s` 等小数秒；倒数表示的相对误差限制在 0.1% 内。
- 焦距优先显示有效的 35mm 等效焦距，缺失、为 0 或无效时回退到实际焦距，两者都无效时省略。
- video 单张照片左右留白相等；多张照片的左右留白和所有图间距相等。
- video 自动寻找能放下照片和水印的最大整数照片高度，保留固定 3840 × 2160 画布；底部横线和日期保留 1.0 的设置，不随照片缩小。
- 防重叠计算包含最右图的两侧占用，以及每对相邻照片的 Logo、签名、参数和地点文字；video 初始图间距小于占用之和 `a` 时，自动缩小照片直到间距至少达到 `1.5 × a`；不重叠时保留原高度。
- adaptive 多图间距、左留白和右留白均为上留白的 1.8 倍（取整到像素），取消张数限制，画布宽度随照片增长。
- Finder 合成入口同步支持 adaptive 多图；video 仍最多 3 张。
- 默认输出文件名过长时自动缩短；超宽 JPEG 提示改用 PNG。

### 优化

- 确定最终布局后仅缩放原图一次，避免反复缩放。
- 普通图片先读取尺寸，绘制时逐张解码并释放，避免同时保留所有原图。
- 文字图片只生成一次，同时用于测量和绘制。
- 每次合成只加载一次品牌匹配规则，统一水印计划和间距计算。
- Finder 入口共用 Python 检查和通知逻辑，失败返回正确状态，文件名通过参数安全传入系统通知。
- 阻止覆盖输入原图；输出先完整写入临时文件再替换，避免失败时损坏已有输出。
- 校验无效 EXIF、GPS、非有限间距及文字边界，移除 Pillow 10+ 已不需要的兼容分支。
- 渲染不回写调用者的配置，允许同一份配置重复使用。
- 保留原有 Finder 脚本路径和命令行参数，无需重建快速操作。

### 文档与展示

- 更新 samples 展示成片，按 video 三个场景、adaptive 三个场景重写 README。
- README 使用独立压缩预览并链接完整成片，新增预览生成脚本。

### 验证

- 自动测试覆盖单图、双图、三图、混合比例、临界重叠、宽 Logo / 地点文字、无水印、EXIF 旋转、长文件名以及 adaptive 多图与超宽布局。
- 从实际导出像素检查 video 等间距和 adaptive 的 1.8 倍上留白间距。
- 使用现有样张验证单图、混合三图 video 和多图 adaptive；Finder 入口以模拟运行验证成功、失败及特殊文件名。
- 回归测试覆盖输出原图保护、保存失败恢复、上下边界及无效参数；69 项自动测试、Ruff 检查及 zsh 语法检查通过。

### 从 1.0 升级

更新整个项目文件夹，包括 `scripts/`、`src/` 和 `assets/`，并重新安装 `requirements.txt` 中的依赖。自行修改过的配置、签名和 Logo 请先备份，再按需迁移。项目路径不变时，原有 Finder 快速操作可继续使用。

## 1.0.0

- 初始版本：照片水印、品牌 Logo、签名、video / adaptive 布局及 Finder 快速操作。
