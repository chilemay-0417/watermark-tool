# 更新日志

安装与升级步骤统一见 [使用指南](docs/usage.md#升级移动与卸载)。以下版本条目记录当时的变化，旧版安装方式不作为当前操作指南。

## 2.2.6 — 2026-09-10

- 补充首次安装后的 Finder 启用指引；双击新装时尝试打开设置，适配 macOS 15 的“登录项与扩展”入口。
- 双击卸载同时移除右键操作和托管终端命令；增加 `--uninstall-finder-only` 保留终端命令。
- 卸载按归属清理未修改且未共用的 PATH 设置，保留用户修改、其他命令、个人素材、照片及已有 Python 环境。
- 补充全新用户目录的安装/卸载、重复安装、配置保护和失败回滚回归测试。

首次安装仍可能需要手动勾选 Finder 操作；本版补充启用步骤和设置入口，不会自动修改系统勾选状态。`--uninstall` 与双击卸载现在同时移除两种入口；需要保留终端命令时请使用 `--uninstall-finder-only`。项目依赖、个人素材、照片和已有 Python 环境保留。

验证范围与限制见 [2.2.6 发布验证](docs/development.md#226-发布验证)。macOS 15.7.7 的设置分支通过模拟测试，首次用户界面尚未在该版本实机验收。

升级：从本版 Release 下载完整项目，先备份并迁移个人配置、签名和 Logo，再重新双击安装。卸载时应使用更新后的脚本。

## 2.2.5 — 2026-09-10

- 双击安装同时配置 Finder 快速操作和 `watermark-tool` 命令；终端可在任意目录使用，共用项目配置与素材。
- 自动查找本机 Python 3.10+ 和 pip，兼容 Homebrew，并补全未激活 Conda 时的目录与环境记录查找。
- 依赖改为安装到工具目录，无需更换 Python 或新建环境；下载、验证失败时保留旧依赖。
- 注册终端命令及 zsh 搜索路径，移动项目后重新双击安装即可更新两种入口。
- 补齐官网 Python 安装目录识别，隔离 pip 安装目标设置，修复新增 SVG 支持时的依赖复用判断。
- Intel Mac 的 16 位 PNG 使用保留精度的 PyPNG 后备解码，避免缺少 pyspng 预编译包导致安装要求编译工具。
- 修复测试代码对 Python 3.10 的兼容性。
- 同步使用、定制和开发说明；README 展示右键功能、终端示例及布局效果，安装步骤集中到使用指南。

验证：Python 3.10 / 3.11 / 3.13 / 3.14 均通过完整测试（每个环境共 186 项，未装 SVG 的环境跳过 1 项）；10 组双架构依赖解析通过，18 组成片与 v2.2.4 保持一致。Intel Mac 和官网 Python 未实机运行，3.12 仅做依赖解析。详细范围见 [发布验证](docs/development.md#225-发布验证)。

升级：备份个人配置和素材，更新完整项目后重新双击安装；不要用旧配置文件直接覆盖新配置。

## 2.2.4 — 2026-09-10

- 安装器直接使用已有 Python 安装依赖，不再新建环境；右键操作固定使用安装时的解释器。
- 精简首页、使用指南、定制说明和开发说明，集中安装步骤，删除重复解释及旧版安装教程。

升级：保留个人配置和素材，更新完整项目后重新双击安装文件。旧 `.venv` 不会自动删除。

验证：166 项测试通过，包含单张、批量、合成导出及原生 Automator 执行；Ruff、Shell 语法、文档链接和差异检查通过。

## 2.2.3 — 2026-09-10

- 内置 PNG Logo 不再需要 Cairo；CairoSVG 改为可选依赖，仅自定义 SVG 使用。
- SVG 缺少依赖时给出安装提示，也可直接改用透明 PNG。
- 修复部分 macOS NumPy 批量矩阵运算导致的色彩转换崩溃，验证样本的量化像素保持一致。
- 整理 Python + pip 安装说明。

验证：原环境 163 项测试通过；无 CairoSVG 的独立 pip 环境通过 162 项，跳过 1 项可选 SVG 测试。实际安装、导出、像素对照及静态检查通过。范围见 [开发说明](docs/development.md#测试与验证)。

## 2.2.2 — 2026-09-10

- 增加对已有 Conda 项目环境的识别，校验解释器所属路径。
- 补充 Cairo 原生库的安装说明。

验证：159 项测试通过；在独立 Conda 环境中验证 SVG、单张、批量和合成导出。

## 2.2.1 — 2026-09-10

- **添加水印**：单选加水印，多选合成为一张。
- **批量添加水印**：逐张加水印，分别导出。
- 两个入口统一安装、卸载，支持旧入口迁移、备份恢复和失败回滚。
- 补充 Finder 菜单排序说明。

升级注意：2.2.0 的「添加水印」多选时逐张导出；从本版起改为合成，逐张导出请用「批量添加水印」。

验证：157 项测试通过，包含实际导出、旧版迁移、原生 Automator 执行和卸载恢复。

## 2.2.0 — 2026-09-10

- 新增中文双击安装、卸载文件，自动配置 Finder 快速操作。
- 当时仅提供「添加水印」入口，单选和多选均逐张导出；多图合成使用命令行。
- 自动备份可识别的旧入口，支持重复安装、失败回滚和同名操作恢复。
- 支持中文、空格和引号等路径字符。

验证：155 项测试通过，其中 12 项覆盖安装器；通过原生 Automator 执行和本机导出验证。

## 2.1.0 — 2026-09-10

- 加快原尺寸导出，减少整幅画布的色彩转换和内存分配；符合条件时直接复制原始整数像素。
- 新增 `--png-compression fast / balanced / small`，均为无损压缩，默认 `balanced`。
- 地点查询结果在本机缓存 30 天，最多 2048 条；可单独关闭或清理。
- 增加照片处理、地点查询和保存阶段的进度提示。
- 拆分布局、品牌、资源和绘制模块，复用元数据读取与原子保存流程。

升级注意：默认布局改为 `adaptive`；固定 4K 请设置 `OUTPUT_MODE = "video"`。对外 Python 入口及命令行保持兼容；直接引用内部函数的代码见 [接口迁移](docs/development.md#内部接口迁移)。

验证：143 项测试通过；36 组样张对照的尺寸、像素、非 ICC 元数据及 ICC 色彩定义一致。本机构造测试图的 JPEG 导出约 7.44 → 0.38 秒，16 位 RGBA 拼接约 4.31 → 1.98 秒；条件与限制见 [性能测量](docs/development.md#本机性能测量)。

## 2.0.0 — 2026-09-09

- 新增 `original` 布局，保留旋正后的照片原始尺寸。
- 默认保留来源 RGB 色域，混合 SDR 色域转为 ProPhoto RGB；可选 sRGB 输出。
- 支持高位深读取、16 位 PNG 输出和透明度保留。
- 默认保留筛选后的拍摄信息等元数据，移除旧缩略图、MakerNote 和设备序列号；多图仅保留共同信息。
- 新增 `--preserve-gps`，默认关闭，与地点联网查询独立。
- 日期只取有效的 EXIF 拍摄时间，缺失时省略。
- 新增 `--batch`，逐张导出，单张失败后继续处理。
- 改进素材缓存、逐张解码、窄图布局和地点查询超时处理。

兼容性变化：停止支持 RAW、TIFF 和 HDR，请先转换为 SDR PNG / JPEG。损坏或不支持的色彩标记会报错；带有效 ICC 的 CMYK / Lab 图片需使用 sRGB 模式。依赖增加 NumPy、pyspng，Pillow 最低版本升至 12.2，移除 rawpy；Python 仍要求 3.10+。

验证：macOS / Python 3.14.2 下 131 项测试通过；未在其他系统或 Python 版本复测。

## 1.1.0 — 2026-09-09

- 修正快门时长，优先显示有效的 35mm 等效焦距。
- video 保持左右留白与图间距相等，拥挤时自动降低照片高度，日期和横线保持指定大小。
- adaptive 取消张数限制，多图间距及左右留白为上留白的 1.8 倍。
- 超长输出文件名自动缩短，超宽 JPEG 提示改用 PNG。
- 减少重复缩放、文字生成、品牌加载及内存占用。
- 阻止覆盖原图；写入成功后才替换成片，失败时保留已有文件。
- 完善无效参数、EXIF、GPS、文字边界和 Finder 失败提示。
- 更新展示样张及文档。

验证：69 项测试、Ruff 和 Shell 语法检查通过，覆盖布局间距、边界、特殊文件名、原图保护及失败恢复。

## 1.0.0

- 初始版本：照片水印、品牌 Logo、签名、video / adaptive 布局及 Finder 快速操作。
